# Refine Skill Checklist

## Purpose
- Use this as the final quick validator after the refinement edits are done.
- This file should stay shorter and more binary than `references/method.md`.

## Final Validation
- Is the `v1` baseline evidence named explicitly?
- Does `SKILL.md` stay at entry-contract level instead of reteaching the full method?
- Does `references/method.md` own the procedure and file-assignment logic?
- Does `references/checklist.md` stay a short final pass rather than a second method document?
- Are file roles now distinct enough that the same fix does not need to be repeated in all three files?
- Was important core skill content preserved, clarified, or moved appropriately rather than deleted?
- Are temporary comparison artifacts either under `docs/tmp/` or absent entirely?
- Does the final response state whether rerun evidence exists, is still missing, or is supplied by the current pass itself?

## Final Question
- If this skill runs again on a similar task, is the same structural cleanup unlikely to be needed again?
