interface:
  display_name: "Init Design"
  short_description: "Turn an initial source set into stable design rules"
  default_prompt: "Use $init-design to turn this initial source set into a durable design constitution."

policy:
  allow_implicit_invocation: true
