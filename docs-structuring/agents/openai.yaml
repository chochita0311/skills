interface:
  display_name: "Docs Structuring"
  short_description: "Organize layered repo documentation"
  default_prompt: "Use $docs-structuring to audit the repository docs, identify duplication and ambiguous ownership, then reorganize them into a clean layered structure with clear source-of-truth boundaries."

policy:
  allow_implicit_invocation: true
