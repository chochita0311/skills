interface:
  display_name: "Project Context Sync"
  short_description: "Sync local multi-repo or single-repo context across sessions"
  default_prompt: "Use $project-context-sync to set or refresh shared context (project root, current repo, related repos, and optional external references) before implementation work."
